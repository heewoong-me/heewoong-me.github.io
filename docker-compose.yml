# Local preview: `docker compose up` -> http://localhost:8080
services:
  jekyll:
    image: amirpourmand/al-folio:v0.14.7
    ports:
      - 8080:8080
      - 35729:35729 # livereload
    volumes:
      - .:/srv/jekyll
    environment:
      - JEKYLL_ENV=development
